// ===================================================================
// 涉密载体管控系统 - 全局防呆规则配置 (随时修改，保存即生效)
// ===================================================================

// 1. 把底层英文变量翻译成中文，方便非技术人员编写规则
def 通行人员 = action.personName
def 通行方向 = action.direction
def 携带的物品列表 = action.tags

// 2. 默认设置：一开始先假定允许放行
def 允许放行 = true

// 3. 核心校验：一件一件地检查带出去的物品
for (物品 in 携带的物品列表) {

    // 【规则 A】：人证必须相符 (拿东西的人，必须是物品的主人)
    if (物品.owner != 通行人员) {
        alert.send("严重违规", "【${通行人员}】试图带走属于【${物品.owner}】的物品：《${物品.name}》！")
        允许放行 = false
    }

    // 【规则 B】：绝密文件禁止出库
//    if (物品.secretLevel == "绝密") {
//        alert.send("最高警报", "检测到绝密级载体：《${物品.name}》！绝密物品严禁离开存放区！")
//        允许放行 = false
//    }
//    if (物品.name == "李四的红头文件") {
//        alert.send("警报", "检测到：《${物品.name}》！该物品严禁离开存放区！")
//        允许放行 = false
//    }
}

// 4. 最终动作执行：根据上面校验的结果，控制闸机
if (允许放行 == false) {
    door.block("存在违规行为，拒绝通过")
} else {
    door.open("人员与物品校验全部合规")
}
